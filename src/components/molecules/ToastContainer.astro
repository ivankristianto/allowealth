---
/**
 * Toast Container Component
 *
 * Container for toast notifications. Place once in your main layout.
 *
 * Usage: Call `showToast(message, type)` from client-side scripts
 */
---

<div id="toast-container" class="toast toast-end toast-top z-50"></div>

<script>
  /**
   * Show a toast notification
   * @param {string} message - The message to display
   * @param {string} [type='info'] - Type of toast: success, error, warning, info
   * @param {number} [duration=5000] - Duration in milliseconds (0 to disable auto-dismiss)
   * @returns {HTMLDivElement|undefined} The toast element or undefined if container not found
   */
  function showToast(message: string, type: string = 'info', duration: number = 5000) {
    const container = document.getElementById('toast-container');
    if (!container) return;

    const toast = document.createElement('div');
    toast.className = `alert shadow-lg alert-${type}`;

    // Use textContent instead of innerHTML for message to prevent XSS
    const messageSpan = document.createElement('span');
    messageSpan.textContent = message; // Automatically escapes HTML

    const closeButton = document.createElement('button');
    closeButton.className = 'btn btn-sm btn-ghost';
    closeButton.setAttribute('aria-label', 'Close toast');
    closeButton.innerHTML = `
      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    `;
    closeButton.onclick = () => toast.remove();

    toast.appendChild(messageSpan);
    toast.appendChild(closeButton);

    toast.style.animation = 'slideIn 0.3s ease-out';
    container.appendChild(toast);

    // Auto-dismiss
    if (duration > 0) {
      setTimeout(() => {
        toast.remove();
      }, duration);
    }

    return toast;
  }

  window.showToast = showToast;
</script>

<style>
  .toast-container > div {
    animation: slideIn 0.3s ease-out;
    margin-bottom: 0.5rem;
  }

  @keyframes slideIn {
    from {
      opacity: 0;
      transform: translateX(100%);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
</style>
