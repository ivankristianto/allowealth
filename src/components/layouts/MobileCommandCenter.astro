---
/**
 * Mobile Command Center Navigation
 *
 * Accessible bottom sheet navigation for mobile with clear visual hierarchy.
 */

import {
  ArrowLeftRight,
  Wallet,
  Donut,
  ChartBar,
  Menu,
  X,
  User,
  Shield,
  LogOut,
  Settings,
  House,
  RefreshCw,
  TrendingUp,
  Calculator,
  ClipboardList,
  ChevronDown,
} from '@lucide/astro';
import type { User as UserType } from '@/lib/auth/lucia';

interface Props {
  currentPath?: string;
  user?: UserType | null;
}

const { currentPath = '/', user = null } = Astro.props;

const isActive = (href: string) => {
  if (currentPath === href) return true;
  if (href === '/dashboard' && (currentPath === '/' || currentPath === '/dashboard')) return true;
  return currentPath.startsWith(href + '/');
};
---

<nav
  class="mobile-command-center fixed inset-x-0 bottom-0 z-[var(--z-fixed)] lg:hidden"
  aria-label="Mobile navigation"
>
  {/* Collapsed Bar */}
  <div
    class="command-bar absolute bottom-4 inset-x-4 bg-linear-to-b from-base-100 to-base-200/90 backdrop-blur-xl rounded-2xl shadow-xl border border-base-300/60 p-2 flex items-center justify-between"
  >
    {/* Menu Button - More visible background */}
    <button
      type="button"
      class="command-menu-btn flex items-center gap-2 px-4 py-2.5 rounded-xl bg-base-300 text-base-content hover:bg-base-300/80 transition-colors border border-base-content/10"
      aria-label="Open navigation"
      aria-controls="command-sheet"
      aria-expanded="false"
      data-menu-toggle
    >
      <Menu size={20} aria-hidden="true" />
      <span class="font-semibold text-sm">Menu</span>
    </button>

    {/* Quick Actions - Increased spacing and touch targets */}
    <div class="flex items-center gap-2">
      <a
        href="/transactions"
        class={`flex items-center justify-center min-h-11 min-w-11 rounded-xl transition-all active:scale-95 ${isActive('/transactions') ? 'bg-accent/15 text-accent shadow-sm' : 'text-base-content/50 hover:text-base-content hover:bg-base-200'}`}
        aria-label="Transactions"
        aria-current={isActive('/transactions') ? 'page' : undefined}
      >
        <ArrowLeftRight size={20} aria-hidden="true" />
      </a>
      <a
        href="/accounts"
        class={`flex items-center justify-center min-h-11 min-w-11 rounded-xl transition-all active:scale-95 ${isActive('/accounts') ? 'bg-accent/15 text-accent shadow-sm' : 'text-base-content/50 hover:text-base-content hover:bg-base-200'}`}
        aria-label="Accounts"
        aria-current={isActive('/accounts') ? 'page' : undefined}
      >
        <Wallet size={20} aria-hidden="true" />
      </a>

      <button
        type="button"
        class="ml-2 w-11 h-11 rounded-full bg-base-100 flex items-center justify-center shadow-lg shadow-base-content/20 hover:shadow-base-content/30 hover:scale-105 active:scale-95 transition-all border border-base-300"
        data-open-transaction-drawer
        aria-label="New transaction"
      >
        <img src="/favicon.svg" alt="" class="w-8 h-8 rounded-full" aria-hidden="true" />
      </button>
    </div>
  </div>

  {/* Full Menu Sheet */}
  <div
    id="command-sheet"
    class="command-sheet fixed left-0 right-0 bottom-0 bg-base-100 rounded-t-4xl shadow-xl transform translate-y-full transition-transform duration-300 ease-out z-[var(--z-modal)] overflow-y-auto overscroll-contain max-h-[85vh] pb-[env(safe-area-inset-bottom)]"
    data-command-sheet
    role="dialog"
    aria-label="Navigation menu"
    aria-modal="true"
    aria-hidden="true"
    inert
  >
    {/* Header */}

    {/* Header - Distinctive accent background */}
    <div
      class="flex items-center justify-between px-5 sm:px-6 py-4 bg-linear-to-r from-accent/10 via-accent/5 to-transparent border-b border-accent/10"
    >
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-xl bg-accent/15 text-accent flex items-center justify-center">
          <Menu size={20} stroke-width={2.5} aria-hidden="true" />
        </div>
        <div>
          <span class="text-lg font-bold text-base-content block leading-tight">Navigation</span>
          <span class="text-xs text-base-content/50">Quick access to all features</span>
        </div>
      </div>
      <button
        type="button"
        class="command-close-btn p-3 rounded-xl bg-base-200 text-base-content/60 hover:text-base-content hover:bg-base-300 active:scale-95 transition-all"
        data-menu-close
        aria-label="Close navigation menu"
      >
        <X size={20} aria-hidden="true" />
      </button>
    </div>

    {/* Main Navigation Grid - Improved active states */}
    <div class="p-4 sm:p-5 grid grid-cols-3 gap-3">
      <a
        href="/dashboard"
        class={`flex flex-col items-center gap-2 p-4 rounded-2xl transition-all active:scale-95 ${isActive('/dashboard') ? 'bg-accent text-accent-content shadow-md shadow-accent/20' : 'bg-base-200 text-base-content/70 hover:bg-base-300 hover:text-base-content'}`}
        aria-current={isActive('/dashboard') ? 'page' : undefined}
      >
        <House size={22} stroke-width={isActive('/dashboard') ? 2.5 : 2} aria-hidden="true" />
        <span class="text-xs font-semibold">Dashboard</span>
      </a>
      <a
        href="/transactions"
        class={`flex flex-col items-center gap-2 p-4 rounded-2xl transition-all active:scale-95 ${isActive('/transactions') ? 'bg-accent text-accent-content shadow-md shadow-accent/20' : 'bg-base-200 text-base-content/70 hover:bg-base-300 hover:text-base-content'}`}
        aria-current={isActive('/transactions') ? 'page' : undefined}
      >
        <ArrowLeftRight
          size={22}
          stroke-width={isActive('/transactions') ? 2.5 : 2}
          aria-hidden="true"
        />
        <span class="text-xs font-semibold">Transactions</span>
      </a>
      <a
        href="/accounts"
        class={`flex flex-col items-center gap-2 p-4 rounded-2xl transition-all active:scale-95 ${isActive('/accounts') ? 'bg-accent text-accent-content shadow-md shadow-accent/20' : 'bg-base-200 text-base-content/70 hover:bg-base-300 hover:text-base-content'}`}
        aria-current={isActive('/accounts') ? 'page' : undefined}
      >
        <Wallet size={22} stroke-width={isActive('/accounts') ? 2.5 : 2} aria-hidden="true" />
        <span class="text-xs font-semibold">Accounts</span>
      </a>
      <a
        href="/budget"
        class={`flex flex-col items-center gap-2 p-4 rounded-2xl transition-all active:scale-95 ${isActive('/budget') ? 'bg-accent text-accent-content shadow-md shadow-accent/20' : 'bg-base-200 text-base-content/70 hover:bg-base-300 hover:text-base-content'}`}
        aria-current={isActive('/budget') ? 'page' : undefined}
      >
        <Donut size={22} stroke-width={isActive('/budget') ? 2.5 : 2} aria-hidden="true" />
        <span class="text-xs font-semibold">Budget</span>
      </a>
      <a
        href="/reports"
        class={`flex flex-col items-center gap-2 p-4 rounded-2xl transition-all active:scale-95 ${isActive('/reports') ? 'bg-accent text-accent-content shadow-md shadow-accent/20' : 'bg-base-200 text-base-content/70 hover:bg-base-300 hover:text-base-content'}`}
        aria-current={isActive('/reports') ? 'page' : undefined}
      >
        <ChartBar size={22} stroke-width={isActive('/reports') ? 2.5 : 2} aria-hidden="true" />
        <span class="text-xs font-semibold">Reports</span>
      </a>
      <a
        href="/recurring"
        class={`flex flex-col items-center gap-2 p-4 rounded-2xl transition-all active:scale-95 ${isActive('/recurring') ? 'bg-accent text-accent-content shadow-md shadow-accent/20' : 'bg-base-200 text-base-content/70 hover:bg-base-300 hover:text-base-content'}`}
        aria-current={isActive('/recurring') ? 'page' : undefined}
      >
        <RefreshCw size={22} stroke-width={isActive('/recurring') ? 2.5 : 2} aria-hidden="true" />
        <span class="text-xs font-semibold">Recurring</span>
      </a>
    </div>

    {/* More Menu Button */}
    <div class="px-4 sm:px-5 pb-3">
      <button
        type="button"
        class="more-menu-btn w-full flex items-center justify-center gap-2 px-4 py-3.5 min-h-11 rounded-xl border-2 border-dashed border-base-300 text-base-content/60 hover:border-base-400 hover:text-base-content hover:bg-base-200/50 transition-all"
        data-more-menu-btn
        aria-expanded="false"
        aria-controls="more-menu-content"
      >
        <span class="text-sm font-medium">More Options</span>
        <ChevronDown size={18} class="transition-transform duration-300" data-chevron-icon />
      </button>
    </div>

    {/* Expanded More Menu */}
    <div
      id="more-menu-content"
      class="more-menu-content hidden px-4 sm:px-5 pb-4 grid grid-cols-3 gap-3"
      data-more-menu-content
    >
      <a
        href="/forecast"
        class={`flex flex-col items-center gap-2 p-4 rounded-2xl transition-all active:scale-95 ${isActive('/forecast') ? 'bg-accent text-accent-content shadow-md shadow-accent/20' : 'bg-base-200 text-base-content/70 hover:bg-base-300 hover:text-base-content'}`}
        aria-current={isActive('/forecast') ? 'page' : undefined}
      >
        <TrendingUp size={22} stroke-width={isActive('/forecast') ? 2.5 : 2} aria-hidden="true" />
        <span class="text-xs font-semibold">Forecast</span>
      </a>
      <a
        href="/calculators"
        class={`flex flex-col items-center gap-2 p-4 rounded-2xl transition-all active:scale-95 ${isActive('/calculators') ? 'bg-accent text-accent-content shadow-md shadow-accent/20' : 'bg-base-200 text-base-content/70 hover:bg-base-300 hover:text-base-content'}`}
        aria-current={isActive('/calculators') ? 'page' : undefined}
      >
        <Calculator
          size={22}
          stroke-width={isActive('/calculators') ? 2.5 : 2}
          aria-hidden="true"
        />
        <span class="text-xs font-semibold">Calculators</span>
      </a>
      <a
        href="https://docs.allowealth.io"
        target="_blank"
        rel="noopener noreferrer"
        class="flex flex-col items-center gap-2 p-4 rounded-2xl bg-base-200 text-base-content/70 hover:bg-base-300 hover:text-base-content transition-all active:scale-95"
        aria-label="Documentation (opens in new tab)"
      >
        <ClipboardList size={22} aria-hidden="true" />
        <span class="text-xs font-semibold">Docs</span>
      </a>
    </div>

    {/* User Section - Improved card style */}
    <div
      class="mx-4 sm:mx-5 mb-6 p-4 bg-linear-to-br from-base-200 to-base-100 rounded-2xl border border-base-300/50"
    >
      <div class="flex items-center gap-3 mb-4">
        <div
          class="w-12 h-12 rounded-full bg-accent/20 text-accent flex items-center justify-center font-bold text-sm ring-2 ring-accent/10"
        >
          {user?.name ? user.name.substring(0, 2).toUpperCase() : '--'}
        </div>
        <div class="flex-1 min-w-0">
          <p class="font-semibold text-base-content truncate">{user?.name || 'User'}</p>
          <p class="text-xs text-base-content/50 truncate">{user?.email || ''}</p>
        </div>
      </div>

      <div class="grid grid-cols-2 gap-2">
        <a
          href="/profile"
          class="flex items-center justify-center gap-2 px-3 py-3 min-h-11 rounded-xl bg-base-100 text-base-content/80 hover:bg-base-300/50 hover:text-base-content transition-all active:scale-95 text-sm font-medium border border-base-300/50"
        >
          <User size={16} aria-hidden="true" />
          Profile
        </a>
        <a
          href="/security"
          class="flex items-center justify-center gap-2 px-3 py-3 min-h-11 rounded-xl bg-base-100 text-base-content/80 hover:bg-base-300/50 hover:text-base-content transition-all active:scale-95 text-sm font-medium border border-base-300/50"
        >
          <Shield size={16} aria-hidden="true" />
          Security
        </a>
        <a
          href="/settings"
          class="flex items-center justify-center gap-2 px-3 py-3 min-h-11 rounded-xl bg-base-100 text-base-content/80 hover:bg-base-300/50 hover:text-base-content transition-all active:scale-95 text-sm font-medium border border-base-300/50"
        >
          <Settings size={16} aria-hidden="true" />
          Settings
        </a>
        <button
          type="button"
          class="logout-btn flex items-center justify-center gap-2 px-3 py-3 min-h-11 rounded-xl bg-error/10 text-error hover:bg-error/20 transition-all active:scale-95 text-sm font-medium"
        >
          <LogOut size={16} aria-hidden="true" />
          Sign out
        </button>
      </div>
    </div>
  </div>

  {/* Backdrop Overlay */}
  <div
    class="command-backdrop fixed inset-0 bg-black/40 backdrop-blur-sm opacity-0 pointer-events-none transition-opacity duration-300 z-[var(--z-modal-backdrop)]"
    data-command-backdrop
    aria-hidden="true"
  >
  </div>
</nav>

<script>
  import { performLogout } from '@/lib/utils/client';

  // Store abort controllers for cleanup
  const navControllers = new Map<HTMLElement, AbortController>();

  function initMobileNav() {
    const commandCenter = document.querySelector('.mobile-command-center');
    if (!(commandCenter instanceof HTMLElement)) return;

    // Cleanup existing listeners if re-initializing
    const existingController = navControllers.get(commandCenter);
    if (existingController) {
      existingController.abort();
    }

    // Create new abort controller for this instance
    const controller = new AbortController();
    const { signal } = controller;
    navControllers.set(commandCenter, controller);

    const menuToggle = commandCenter.querySelector('[data-menu-toggle]');
    const menuClose = commandCenter.querySelector('[data-menu-close]');
    const sheet = commandCenter.querySelector('[data-command-sheet]');
    const backdrop = commandCenter.querySelector('[data-command-backdrop]');
    const logoutBtn = commandCenter.querySelector('.logout-btn');
    const moreMenuBtn = commandCenter.querySelector('[data-more-menu-btn]');
    const moreMenuContent = commandCenter.querySelector('[data-more-menu-content]');
    const chevronIcon = commandCenter.querySelector('[data-chevron-icon]');

    if (!(sheet instanceof HTMLElement)) return;
    if (!(backdrop instanceof HTMLElement)) return;
    if (!(moreMenuBtn instanceof HTMLButtonElement)) return;
    if (!(menuToggle instanceof HTMLElement)) return;

    // Store last focused element to restore focus on close
    let lastFocusedElement: HTMLElement | null = null;

    // Cache for focusable elements to avoid recomputing on every Tab key
    let cachedFocusableElements: HTMLElement[] | null = null;

    // Get all visible focusable elements within the sheet (exclude hidden/disabled)
    const getFocusableElements = () => {
      // Return cached result if available
      if (cachedFocusableElements !== null) {
        // Filter out any elements that may have become hidden since cache
        return cachedFocusableElements.filter((el) => el.closest('.hidden') === null);
      }

      const allFocusable = sheet.querySelectorAll<HTMLElement>(
        'a[href], button:not([disabled]), input:not([disabled]), textarea:not([disabled]), select:not([disabled]), details, [tabindex]:not([tabindex="-1"])'
      );
      cachedFocusableElements = Array.from(allFocusable).filter((el) => {
        // Check if element is actually visible (not in a hidden parent)
        const isInHiddenParent = el.closest('.hidden') !== null;
        return !isInHiddenParent && !el.hasAttribute('inert');
      });
      return cachedFocusableElements;
    };

    // Invalidate cache when more menu is toggled
    const invalidateFocusCache = () => {
      cachedFocusableElements = null;
    };

    const openSheet = () => {
      // Store current focus
      lastFocusedElement = document.activeElement as HTMLElement;

      sheet.classList.remove('translate-y-full');
      sheet.classList.add('translate-y-0');
      sheet.removeAttribute('inert');
      sheet.removeAttribute('aria-hidden');
      backdrop.classList.remove('opacity-0', 'pointer-events-none');
      backdrop.classList.add('opacity-100', 'pointer-events-auto');
      document.body.style.overflow = 'hidden';

      // Update aria-expanded on toggle
      menuToggle.setAttribute('aria-expanded', 'true');

      // Focus the close button after transition
      setTimeout(() => {
        const closeBtn = sheet.querySelector('[data-menu-close]') as HTMLElement | null;
        closeBtn?.focus();
      }, 100);
    };

    const closeSheet = () => {
      sheet.classList.add('translate-y-full');
      sheet.classList.remove('translate-y-0');
      sheet.setAttribute('inert', '');
      sheet.setAttribute('aria-hidden', 'true');
      backdrop.classList.add('opacity-0', 'pointer-events-none');
      backdrop.classList.remove('opacity-100', 'pointer-events-auto');
      document.body.style.overflow = '';

      // Update aria-expanded on toggle
      menuToggle.setAttribute('aria-expanded', 'false');

      // Reset more menu when closing sheet
      moreMenuContent?.classList.add('hidden');
      chevronIcon?.classList.remove('rotate-180');
      moreMenuBtn.setAttribute('aria-expanded', 'false');

      // Restore focus to the toggle button
      setTimeout(() => {
        lastFocusedElement?.focus();
        lastFocusedElement = null;
      }, 100);
    };

    menuToggle?.addEventListener('click', openSheet, { signal });
    menuClose?.addEventListener('click', closeSheet, { signal });
    backdrop?.addEventListener('click', closeSheet, { signal });

    // Toggle more menu
    moreMenuBtn.addEventListener(
      'click',
      () => {
        const isExpanded = moreMenuBtn.getAttribute('aria-expanded') === 'true';
        moreMenuContent?.classList.toggle('hidden');
        chevronIcon?.classList.toggle('rotate-180');
        moreMenuBtn.setAttribute('aria-expanded', String(!isExpanded));
        // Invalidate focus cache since visibility changed
        invalidateFocusCache();
      },
      { signal }
    );

    // Focus trap within the sheet
    sheet.addEventListener(
      'keydown',
      (e) => {
        if (e.key !== 'Tab') return;

        const focusableElements = Array.from(getFocusableElements());
        if (focusableElements.length === 0) return;

        const firstElement = focusableElements[0];
        const lastElement = focusableElements[focusableElements.length - 1];
        const currentElement = document.activeElement as HTMLElement;

        if (e.shiftKey) {
          // Shift+Tab: moving backwards
          if (currentElement === firstElement) {
            e.preventDefault();
            lastElement.focus();
          }
        } else {
          // Tab: moving forwards
          if (currentElement === lastElement) {
            e.preventDefault();
            firstElement.focus();
          }
        }
      },
      { signal }
    );

    // Close sheet when clicking any link inside it (including external links)
    sheet.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', closeSheet, { signal });
    });

    // Logout handler
    logoutBtn?.addEventListener(
      'click',
      async (e: MouseEvent) => {
        e.preventDefault();
        await performLogout();
      },
      { signal }
    );

    // Close on escape key
    document.addEventListener(
      'keydown',
      (e) => {
        if (e.key === 'Escape') closeSheet();
      },
      { signal }
    );

    // Cleanup when element is removed from DOM
    const observer = new MutationObserver((mutations) => {
      mutations.forEach((mutation) => {
        mutation.removedNodes.forEach((node) => {
          if (node === commandCenter || (node instanceof Element && node.contains(commandCenter))) {
            controller.abort();
            navControllers.delete(commandCenter);
            observer.disconnect();
          }
        });
      });
    });

    observer.observe(document.body, { childList: true, subtree: true });

    // Also cleanup on view transition
    document.addEventListener(
      'astro:before-swap',
      () => {
        // Reset scroll lock and sheet state before view transition
        document.body.style.overflow = '';
        controller.abort();
        navControllers.delete(commandCenter);
        observer.disconnect();
      },
      { signal }
    );
  }

  // Run on load and after view transitions
  initMobileNav();
  document.addEventListener('astro:page-load', initMobileNav);
</script>
