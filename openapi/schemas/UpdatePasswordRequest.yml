# Update Password Request Schema
UpdatePasswordRequest:
  type: object
  required:
    - oldPassword
    - newPassword
  properties:
    oldPassword:
      type: string
      minLength: 1
      description: Current password for verification (only checked against stored hash)
      example: SecurePass123!
    newPassword:
      $ref: './Password.yml#/Password'
